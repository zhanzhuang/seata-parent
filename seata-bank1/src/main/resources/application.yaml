spring:
  profiles:
    active: dev
  application:
    name: seata-bank1

---
spring:
  profiles: dev
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/seata-bank1?characterEncoding=utf8&allowMultiQueries=true&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456
  application:
    name: seata-bank1
server:
  port: 8081
mybatis-plus:
  mapper-locations: calsspath:mapper/*.xml